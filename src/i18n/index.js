import { LOCALE_SELECTED, localeReducer } from './reducer';

import { selectedLocale } from './actions';

import IntlReduxProvider from './intl-redux-provider';

export {
  LOCALE_SELECTED, localeReducer, selectedLocale, IntlReduxProvider
}
